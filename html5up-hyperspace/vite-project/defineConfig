import { defineConfig } from 'vite'

export default defineConfig({
  // ta configuration ici, par exemple
  server: {
    port: 3000,
  },
  build: {
    outDir: 'dist',
  },
  // ajoute d’autres options selon tes besoins
})
